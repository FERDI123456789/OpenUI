---
const { userId } = Astro.params;

const Provider = process.env.PUBLIC_CONVEX_URL;

import Layout from "../../layouts/Layout.astro";
import Main from "../../components/main";
---

<Layout>
    <Main userId={userId} Provider={Provider} client:load />
</Layout>
