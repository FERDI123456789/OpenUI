---
export const prerender = false;

const { id } = Astro.params;
console.log("Astro received id from params:", id); // Server-side debug log - check terminal/build logs

import SPubKompWrapper from "../../components/sPubKompWrapper"; // Add .tsx if needed for your setup
import Layout from "../../layouts/Layout.astro";
---

<Layout>
  <SPubKompWrapper id={id} client:load /> <!-- Use client:load for faster hydration if possible -->
</Layout>